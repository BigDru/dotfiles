set number
set numberwidth=5

set cursorline

set hlsearch
set incsearch

set noswapfile

set autoindent
set tabstop=4 "visually, how many cols a tab should use
set expandtab "expands tab into spaces
set shiftwidth=0 "controls how many cols text is indented with >> and <<. 0 means cols shifted = tabstop number
set softtabstop=0 "if different from tabstop, spaces are used to make the difference. 0 means this feature is off
